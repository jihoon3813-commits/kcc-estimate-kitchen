{
  "name": "kcc-kitchen-estimate",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx vite",
    "build": "mkdir -p dist && cp -r * dist || true",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "convex": "^1.34.1"
  }
}
